import type { ReactNode } from "react";import { InlineAlert } from "../../../design-system/primitives";
export function WorkItemCollectionLayout({inspectorOpen,children,inspector}:{inspectorOpen:boolean;children:ReactNode;inspector?:ReactNode}){return <div className={`grid min-h-0 flex-1 grid-cols-[minmax(0,1fr)] overflow-hidden ${inspectorOpen?"relative block md:grid md:grid-cols-[minmax(0,1fr)] lg:grid-cols-[minmax(0,1fr)_clamp(360px,28vw,440px)]":""}`}><section className={`min-h-0 min-w-0 overflow-hidden ${inspectorOpen?"max-sm:invisible":""}`}>{children}</section>{inspector}</div>}
export function WorkItemCollectionLoading({label="Loading Work Items"}:{label?:string}){return <div className="pt-[30px]" aria-label={label}>{Array.from({length:10},(_,i)=><div key={i} className="grid h-[var(--table-row-height)] grid-cols-[76px_1fr_96px_118px] items-center gap-2 border-b border-[var(--border-subtle)] px-2">{Array.from({length:4},(_,j)=><span key={j} className="h-2 rounded bg-[var(--surface-subtle)]"/>)}</div>)}</div>}
export function WorkItemCollectionFailure({children="Could not load Work Items. Your collection context is preserved."}:{children?:ReactNode}){return <InlineAlert role="alert">{children}</InlineAlert>}
export function WorkItemCollectionStale({children}:{children:ReactNode}){return <InlineAlert>{children}</InlineAlert>}
export function WorkItemCollectionEmpty({title,description,action}:{title:string;description:string;action?:ReactNode}){return <div className="grid h-full place-content-center gap-1 text-center text-caption text-[var(--text-secondary)]"><strong>{title}</strong><span>{description}</span>{action}</div>}
