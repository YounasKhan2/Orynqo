export function MutationStateLabel({phase}:{phase:string}){if(phase==="idle"||phase==="settled")return null;const emphasis=phase==="conflict"||phase==="failure"?"font-semibold":"";return <span className={`inline-flex min-h-5 items-center rounded-full border border-[var(--border-subtle)] px-1.5 text-caption capitalize ${phase==="pending"?"border-dashed":""} ${emphasis}`} role="status">{phase==="pending"?"Pending":phase==="offline"?"Offline · unacknowledged":phase}</span>}