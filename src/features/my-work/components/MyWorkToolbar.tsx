import { Search } from "lucide-react";
import type { MyWorkGroupMode,MyWorkSort,MyWorkStatusFilter } from "../model";
type Props={q?:string;status:MyWorkStatusFilter;group:MyWorkGroupMode;sort:MyWorkSort;count:number|undefined;onStatus:()=>void;onGroup:(value:MyWorkGroupMode)=>void;onSort:(value:MyWorkSort)=>void;onSearch:(value:string)=>void};
export function MyWorkToolbar({q,status,group,sort,count,onStatus,onGroup,onSort,onSearch}:Props){return <div className="work-toolbar" aria-label="My Work collection controls"><button type="button" className="filter-chip" aria-pressed={status==="not-done"} onClick={onStatus}>Status ≠ Done</button><label className="my-work-control"><span>Group</span><select aria-label="Group My Work" value={group} onChange={e=>onGroup(e.target.value as MyWorkGroupMode)}><option value="due">Due window</option><option value="project">Project</option></select></label><label className="my-work-control"><span>Sort</span><select aria-label="Sort My Work" value={sort} onChange={e=>onSort(e.target.value as MyWorkSort)}><option value="priority">Priority</option><option value="key">Key</option><option value="title">Title</option></select></label><label className="search-field my-work-search"><Search size={14}/><span className="sr-only">Search My Work</span><input value={q??""} placeholder="Search issues" onChange={e=>onSearch(e.target.value)}/></label><span className="collection-count">{count??"—"} issues</span></div>}
